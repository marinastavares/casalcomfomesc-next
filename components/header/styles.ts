import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  header: {
    position: 'absolute',
    top: -8,
    right: -8,
    left: -8,
  },
}));


export default useStyles
